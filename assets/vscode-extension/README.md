# texe Paper Layout

This bundled companion opens a newly created texe paper with its LaTeX source
on the left and LaTeX Workshop's PDF viewer on the right. It performs no
builds, networking, telemetry, or background work.
