# texe Paper Layout

This bundled companion opens a texe paper with its LaTeX source on the left and
LaTeX Workshop's PDF viewer on the right. If the first PDF has not been built
yet, it adds the PDF as soon as it appears. Run **texe: Open Paper Side by
Side** from the Command Palette to restore the layout at any time.

The companion performs no builds, networking, or telemetry.
